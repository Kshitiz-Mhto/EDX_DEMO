name: Python type check

on:
  push:
    path:
      - "**.py"

jobs:
  pyre-on-work:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Pyre Action
        uses: facebook/pyre-action@v0.0.1
